apiVersion: v2
name: secrets
description: K8s Secrets for govatars stack (infra + app). Passwords generated on first install, preserved on upgrade.
type: application
version: 0.1.0
appVersion: "1.0.0"
